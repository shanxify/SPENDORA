{
  "name": "money-tracker",
  "version": "1.0.0",
  "description": "PhonePe transaction tracker",
  "scripts": {
    "start": "cd frontend && npx vercel dev",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "playwright": "^1.61.1"
  }
}
